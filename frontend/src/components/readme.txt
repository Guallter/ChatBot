/*



Na pasta de components deve-se colocar elementos que se repetem e que pode vir a auxiliar na praticidade.

Exemplo:

Cabeçalho - Se todas paginas vão ter um, ele pode ser um componente que você chama nas páginas,
evitando a reescrita de codigos.


Exemplo:

Caso queira chamar o HEADER em uma Página, basta:

import Header from "../../component/Header";

function -nomedafunção- (){
    return(
        <Header />               ------Aqui é feito a chamada 
    );
}

Ou seja, basta dar o IMPORT e chamar.



*/
